import {StyleSheet} from 'react-native';

export const dividerStyle = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 24,
  },
  line: {
    flex: 1,
    height: 1,
  },
  orText: {
    fontWeight: 'bold',
    marginHorizontal: 8,
  },
});
